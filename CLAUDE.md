# CLAUDE.md

このリポジトリで作業するときに最初に読む文書。

## この作品は何か

日本列島を舞台にしたグランドストラテジー。**領土の塗り替えではなく、
人口・産業・物流・政治をどう維持するか**が主題（企画書 `docs/design.md` §2）。

人間・ヒューリスティック AI・LLM・強化学習エージェントが、
同じ `Observation → Action` のインターフェースで同じ世界に触れる（§14）。

## 必ず読むもの

| 文書 | 内容 |
|---|---|
| `docs/conventions.md` | **コーディング規約。拘束力がある。作業前に必ず読む** |
| `docs/design.md` | 企画書。何を作ろうとしているか |
| `docs/future-work.md` | 検討して見送った判断と残件。**同じ議論を繰り返さないための記録** |
| `docs/mvp-spec.md` 〜 `docs/phase11-spec.md` | 各段階の実装仕様と受け入れ基準 |

## 構成

```
crates/sim/     シミュレーションコア（外部クレート依存 0・Bevy 非依存・完全決定論）
                地域が政治・経済の単位、輸送網（transport.rs）が補給の流路
                航空（air.rs）は飛行場ノードとして輸送網に参加する
crates/agents/  ヒューリスティック / LLM / Human エージェント（Bevy 非依存）
crates/llm/     LLM バックエンド（OpenAI 互換 HTTP、TLS なし＝ローカル/プロキシ経由）
crates/api/     REST / WebSocket（WebSocket も自前実装、依存 0）
apps/headless/  描画なしシミュレーター
apps/game/      Bevy クライアント（Bevy が入るのはここだけ）
python/env/     Gymnasium 互換の強化学習環境
scenarios/      mvp.json（10 地域）/ japan47.json（47 都道府県）/ japan_hex.json（289 マス）
```

**ワークスペース全体が外部クレート依存 0。** Bevy は `apps/game` にのみ入る。
`cargo build --workspace --exclude archipelago-game` が通ること。

### 輸送網（Phase 9）

`Region` は政治・経済の単位のまま。**地域リンク（`Region::links`）は部隊の
移動専用、補給は輸送網（`crates/sim/src/transport.rs`）の上を流れる。** 別レイヤー。

```
Junction  分岐点。それ以外の役割はない
Depot     補給拠点。所属地域の部隊へ払い出す
Port      港。封鎖の対象。「港があるか」の根拠はノードの存在そのもの
          （Region::port は量だけを持つ。両者の不一致は validate が弾く）
```

補給は容量制約つきの**流量**（`logistics::compute_transport_flow`）。配分計算
と反映を分けた同期 2 パスで、固定 24 ラウンド（`SUPPLY_FLOW_ROUNDS`）反復する。
48・96 ラウンドとの比較で 24 が全桁一致の 1% 以内に収まることを測って決めた値で、
`balance.rs` にその測定を記録している——ラウンド数を増やしても飢えは直らない。

`world.supply[region]` の意味は**「その地域が引ける上限」から「実際に流れた量」
に変わった。** 需要で頭打ちになるので、部隊のいない地域は 0 になる。

### 航空戦力（Phase 10）

飛行場は `Region` の属性ではなく輸送網のノード（`transport::TransportNodeKind::
Airfield`）。**航空部隊の補給は最初から `compute_transport_flow` に入る。**
Phase 9 で最も高くついた欠陥は艦隊だけが有限流量の外にいたことだった
（消費されない容量を別経路で渡し、同じ港の 2 個艦隊が満額を受け取っていた）。
同じ形を航空で繰り返さないため、10A の受け入れ基準に「同じ飛行場の 2 個航空部隊
が容量を分け合う」を最初から入れた（`crates/sim/src/air.rs`）。

制空権（`Region::air_superiority`）は地域に対して立ち、両勢力の投入戦力の比率で
決まる。作戦半径（`AIR_OPERATING_RADIUS_KM` = 300km）は輸送網上の距離ではなく
`Region::position` の**地理的距離**——航空機は線路の上を飛ばない。**これにより
`Region::position` はもう飾りではなくなった。** `mvp.json`・`japan47.json` の
座標も、Phase 7A 以来の尺度のない模式的な配置から、県庁所在地を基準にした実
キロメートル（`tools/rescale_positions.py`、japan_hex と同じ投影）に置き換えた
——AI が全シナリオで航空部隊を飛ばすようになった時点で、模式的な配置では
制空権が事実上全域に及んでしまっていたため。

制空権は輸送路線の阻止（`Action::InterdictLine`）と飛行場・港ノードへの一撃
（`Action::StrikeNode`）の両方に効く。**空襲は一方的ではない。** 目標の防空が
強いほど攻撃側の戦果は減衰し（`air::air_superiority_factor`）、出撃した航空部隊
自身もその防空の強さに比例して損耗する（`air::apply_strike_losses`）——制空権を
取らずに飛行場を叩くことに代償を持たせている。

**意図的に持たないもの**: 爆撃機/戦闘機の区別、地上戦への直接支援（§0 が阻止を
主任務に選んだ）、天候。

## 動かす

```sh
# 観る（AI 同士）
cargo run --release -p archipelago-game -- --scenario scenarios/japan47.json --seed 2

# 遊ぶ
cargo run --release -p archipelago-game -- --play 東方連合

# 描画なし
cargo run -p archipelago-headless -- --seed 1 --days 720 --newspaper
cargo run -p archipelago-headless -- --seed 1 --days 720 --bench

# API / 強化学習
cargo run -p archipelago-api -- --bind 127.0.0.1:8080
cd python && pytest

cargo test --workspace     # 404 件
```

クライアントのキー: `N` 新聞 / `L` 補給網 / `D` 外交 / `Tab` 勢力切替 /
`Space` 一時停止 / `1` `2` `3` 速度。`--days` で日数上限（既定 720）。

## 最重要の不変条件

### ハッシュ固定はやめた（経緯を残す）

かつて `scenarios/mvp.json` seed 1 / 720 日の `--json` を sha256 で固定し、
Phase 1 から不変であることを最重要の不変条件として扱っていた。**廃止した。**

**廃止の理由。** ハッシュは振る舞いのテストではない。digest を見ても何の性質を
守っているか分からず、失敗しても「何かが変わった」としか言わない。出力形式に
結合しており、JSON の書式を変えただけで壊れる。そして**設計を歪めた**。

実際に起きた歪み 3 件（いずれも差し戻し・修正済み）:

- 勢力 index 7 だけを特別扱いする定数をエンジンに埋め込む案。index を選んだ
  理由が「mvp に 7 番目の勢力がいないから」だった
- 部隊上限を軍需品生産から導く案。控除の値が mvp の生存を基準に決められ、
  結果 mvp が決着する戦争から膠着に変わった
- `INDUSTRIAL_STABILITY_FLOOR` を 0.7 に設定。コメント自身が「mvp の実測最小
  0.796 を下回るので no-op」と認めていた。一度も発火しない床は、規約 §6 が
  要求する回復経路を与えていない

**失ったもの。** ハッシュの本当の価値は、あらゆる tick システムの、あらゆる
理由による、意図しない挙動変化を端から端まで捕まえる網だった。これは有限個の
名前付きテストに分解できない。実際、`enemy_power` を `total - own` と書き換えた
最適化は数学的に等価でビット単位では等価でなく、AI の判断を 300 日後に反転させた
が、名前付きテストは全部通っていた。**同種の見落としは今後は検出できない。**

**代わりに残したもの。**

- `determinism` テスト（同一 seed で 2 回実行して一致）。RL の再現性と
  リプレイという製品上の保証を守る、名前で意図が分かるテスト
- `power_table_matches_direct_computation_bit_for_bit`。上記の浮動小数の
  再結合という具体的な欠陥クラスを名前のある性質として書き直したもの

### 却下した codex review の指摘（再議論しない）

輸送網の到着分配（`commit_instantaneous_land_grant`/`_sea_grant`）が、同じ
tick に複数部隊が到着したとき先に処理された行動を有利にする、という指摘を
`codex review` は毎回 P1 で出す。**毎回却下している。** 規約が禁じるのは
意思決定で動かせない engine 側の固定優先順位であり、行動の処理順はプレイヤー
と AI 自身が決めるものだからである。`Faction::stock[Arms]`・`manpower` も
同じ行動処理順で消費しており、ここだけ特別扱いする理由がない。理由の全文は
`crates/sim/src/logistics.rs` の `commit_instantaneous_land_grant` に記録した。

### 決定論

同じ seed と同じ入力列なら必ず同じ結果になる。
`HashMap` / `HashSet` の反復順に依存しない。浮動小数の加算順序を固定する。

描画・API・LLM のいずれもシミュレーションを変えてはならない。
検証済みの同一性:

```
--agent llm --backend fail  ==  --agent heuristic     LLM が落ちてもゲームは変わらない
--newspaper あり/なし        ==                        新聞は純粋な出力
API 経由  ==  headless                                 RL の再現性の根幹
```

## 繰り返し踏んだ欠陥（Phase 1〜6 で 12 件、Phase 9 でさらに 3 件、Phase 10 でさらに 4 件）

すべて同じ形だった。**新しいコードでも必ず警戒する。** Phase 9 は輸送網を
「補給が有限流量になっていない」という、まさにこの節が禁じる形の欠陥として
着手し、直している最中に同じ形をもう 3 回踏んだ。

- 希少な資源に**固定の優先順位**を置かない。必ず比率で按分する。
  意思決定で動かせないループは境界値で飽和する
- **一方通行のアキュムレータ**を作らない。増える量には戻る経路を持たせる
- 1 tick の許容量は**減る予算**として持つ。残量に比率を掛け直す実装は
  行動の連打で破られる（RL エージェントが操作する前提）。Phase 9 でも踏んだ:
  到着済み部隊の補給をその場で再計算する修正が、tick の残り予算ではなく
  **フル容量から**計算し直しており、同時到着 N 部隊がちょうど N 倍受け取れた
- **発令時点の値を焼き込まない。** 現在の状態を追うべき値を一度だけ標本化しない
- 状態には**必ず回復経路**を持たせる。入ったら出られない状態を作らない
- **フィールドの意味を変えるのはマイグレーションである。** Phase 9 で
  `world.supply[region]` を「引ける上限」から「実際に流れた量」に変えたとき、
  読み手の移行を 3 回連続で取りこぼした（`distribute_supply` の非所有分岐・
  `land_unit_supply_avail`・`fleet_unit_supply_avail`）。うち 1 回は読み手を
  明示的に確認したうえで「対象外」と誤って判定していた。効いたのは読み手を
  **全部列挙し**、1 つずつ「流れた量」と「網が運べる量」のどちらを欲しいかを
  判定すること
- **勢力ごとの事実にしたのに、隣接する事実を共有構造に焼き込んだままだと
  修正は半分しか効かない。** Phase 9 で占領軍への中継可否（`contested`）と
  制海権判定を勢力ごとにしたが、法的所有者の視点のままの参照が並存していた
- **行動の前提条件は、対象だけでなく実行側も検証する。** `StrikeNode` は目標が
  敵の飛行場・港かだけを見ており、実行側が航空機を持つかを見ていなかった。
  `InterdictLine` も実行側の部隊の有無を見ていなかった。AI は自分から到達できる
  範囲しか狙わないので自己遮蔽していて、人間/RL の経路ができるまで露出しなかった。
  前提条件には**実行側が実際に到達できるか**を必ず含める
- **穴を焼き込んだテストが 9 件あった。** 攻撃側の航空部隊を 1 つも置かずに
  空襲を検証しており、うち 1 件はコメント自身が「攻撃側の部隊がどこにもいない」
  と書いていた。穴を運動させるテストは、その穴を仕様として固定してしまう
- **「地域はこれを持っているか」と「使えるものをくれ」は同じ検索を共有しない。**
  最小 id のノードを返す実装が、地域が同種のノードを 2 つ宣言すると 4 か所
  （輸入・徴募・AI の再配置・クライアントの空襲パネル）で誤っていた
- **閾値の緩和は 1 度まで。2 度目はラチェットであってテストではない。**
  composite の hawk/dove の複合判定は一度緩めたあと再び境界値で落ち、比較
  対象そのものも誤っていた。緩めずに削除し、性質は残りの assertion が
  守っていることを確認した

## 検証についての教訓

このリポジトリで実際に起きた失敗。**代理指標を測って本質を測り損ねた**例が 3 件。

1. **空のテストが 2 件あった。** 所有者を変えて補給を測る回帰ガードは、
   `recompute_supply` が勢力をまたぐ辺を元々通さないため、
   機構が壊れていても緑のままだった。うち 1 件は Phase 1 から存在した
2. **画素統計が可読性を測らなかった。** 色数・非背景画素率・連結成分数は
   すべて妥当な値を返しながら、日本語は全部豆腐で地図は画面外に切れていた
3. **検証条件が使用条件とずれていた。** スクリーンショットが全部拡大した
   切り抜きで、プレイヤーが実際に見る既定ズームではなかった

したがって:

- テストは**落ちうることを確認してから**信用する。機構を壊して失敗させてみる
- 画面は**見る**。数えない
- 検証は**実際の使用条件**で行う

## 作業の進め方

1. `docs/conventions.md` を読む
2. 仕様を書く（受け入れ基準まで含めて）
3. 実装する
4. `codex review --uncommitted` を通し、指摘を解消する
5. mvp のハッシュとテストを確認してコミット

段階ごとに小さく着地させる。Phase 2〜7 はすべてこの形で進めた。
測定してから直す。推測で最適化しない（Stage 6C は測定の結果、
想定していた BFS ではなく外交判断が真因だった）。

## 現状

企画書 §20 の Phase 1〜10、Bevy クライアント（§16）、ヘクスグリッドの日本地図・
輸送網（§3・§8・§21-2）・航空戦力（§10）を実装済み。404 テスト、警告 0、
約 52,000 行。

### 3 つの地図

| シナリオ | 規模 | 位置づけ |
|---|---|---|
| `scenarios/mvp.json` | 10 地域・3 勢力 | 参照実装。8 seed 中 7 が決着する戦争が成立している（234〜463 日、seed 2 のみ膠着） |
| `scenarios/japan47.json` | 47 県・6 勢力 | **経済が実質破綻していて陸戦がほぼ起きない。** 比較用に残す |
| `scenarios/japan_hex.json` | 289 マス・8 勢力 | **本命。** 標高データから地形を、実データから輸送網を導出した実地図 |

`japan47.json` は生産能力表を mvp の全国合計に合わせて作り、6 分割された場合を
検証しなかったため、全勢力が長期間 munitions ≈0 のまま推移する（陸戦 6、
占領 8〜10、海戦は seed によって 0〜4）。Phase 10 で座標が実キロメートルになり
制空権の半径が意味を持つようになった結果、以前の 720 日凍結ではなく、初期の
宣言同盟がそのまま生き残って連合勝利に至るようになった（day 231〜315）。
**戦争がほぼ起きないという欠陥自体は変わっていない。** japan_hex がこれを
置き換えた（陸戦 79〜108）。japan47 を直す価値は薄いが、回帰の比較対象として
残している。

### japan_hex の状態

地形は国土地理院の標高タイルから導出。山地 22.8% / 丘陵 50.5% / 平地 26.6% で、
山地と丘陵の合計 73.4% は実際の日本とほぼ一致する。日本アルプス・紀伊山地・
北上・中国山地・四国山地・阿蘇が山地、関東・濃尾・大阪・石狩・筑紫が平地、
甲府盆地が山に囲まれた丘陵として出ている。関門はトンネル（封鎖免疫）、
青函と瀬戸内は海峡。

輸送網は Phase 9C で実データに差し替え済み（国土数値情報の鉄道・港湾データ、
後述のライセンス節を参照）。Phase 10A で飛行場ノード 64 を追加した——国土数値
情報の空港データ（C28-21）から導出し、289 マス中 64 マスが実在の空港と 50km
以内で一致、外れた 25 は離島としてログに残している。532 ノード（補給拠点 289・
港 179・飛行場 64）・917 路線（鉄道 569・道路 342・海上 6）。新幹線・幹線が
九州→山陽→東海道→東北→北海道の主幹を通り、関門は実在の新幹線・鹿児島本線
由来の Rail（封鎖免疫）、青函・瀬戸内は港と港を結ぶ Sea。もう仮置きではない。

戦争は成立している（陸戦 79〜108・海戦 124〜180・占領 155〜157 / seed）。
3 seed とも 720 日で膠着だが、これは仕様である（後述）。永久破綻もない
（軍需品ゼロ近傍の最長連続 94〜96 日、受け入れテストの基準は 250 日）。

**この数字は一度 96〜186 と誤記していた。** 文書更新時に報告された値を自分で
確かめずに書いたもので、「余裕が半減した」という記述も誤りだった。正しくは
Phase 9 時点とほぼ同じである。受け入れテスト自身の計算
（`assert_no_faction_is_permanently_insolvent` が使う `Trajectory::
max_insolvent_streak_days`。脱落した勢力は 0 に戻す）を読んで取り直した。
**このファイルの数字は、書く前に自分で走らせて確かめること。**

### japan_hex のデータのライセンス

鉄道データ（N02-24）は CC BY 4.0。出典表示は `scenarios/japan_hex.json` の
`transport.note` に入れてあり、配布物に付いて回る。港湾データ（C02-14）は
旧規約の**非商用**ライセンスで、生成物に入るのは港の等級と名前だけ（座標・
図形は入らない）。**このデータのまま商用展開はできない。** 商用化するなら
港の等級を Phase 8 の地形と同じく海岸線と人口から導出し直す必要がある。
2026-09-05、相談のうえこのまま進めると判断した。飛行場データ（C28-21）は
商用可のライセンスで、この制約を受けない。

## テスト

```sh
cargo test --workspace                    # 404 件、約 15 秒
cargo test -p archipelago-headless --test scenario_acceptance -- --ignored
cargo test -p archipelago-game --test scenario_acceptance -- --ignored
```

**シナリオ受け入れテストが最も重要。** 単体テストは機構を個別に検証するが、
シナリオを端から端まで遊んだときに遊ぶに値するゲームになっているかは、
これだけが検証する。今日の重大な欠陥はすべて手で遊んで発見されており、
236 件の単体テストは 1 つも捕まえなかった。

検査する性質:

- 戦争が起きている（戦闘・占領・戦死が閾値以上）
- 永久破綻がない（軍需品ゼロの連続日数に上限）
- 世界が動いている（部隊数と領土が長期間凍結しない）
- プレイヤーの判断が効く（`--replay` で苦境の勢力を救う操作が効果を持つ）
- 決定論（replay 適用時も含む）
- 脱落した勢力をそう表示する、同梱フォントが全文字を被覆する

**許容幅は広く取る。** 定数が動くたびに落ちるテストは雑音で、
ゲームがゲームでなくなったときだけ落ちるのが信号である。

**自動化できないもの**（テストファイル自身にも明記）: 地図が日本に見えるか、
配色が読めるか、トラックパッドの操作感。これらは人が見るしかない。

### 企画書 §2 の因果 — 3 つは成立、1 つは成立しない

- 東海・関東の機械産業を失うと、鉄鋼が余っていても全国の装備生産が止まる
- 港湾を封鎖された勢力は輸入が止まり、都市圏を自給できずに飢える
- 統治できない占領地は独立運動で流れ戻る

**「戦争に勝っていても、徴兵と配給の締め付けを続ければ政権が倒れる」は
成立していない。** かつてここに成立していると書いていたが誤りだった。

政策の経路から徴兵 1.0・配給 0.5 を 720 日与えても `stability` は
`REGIME_CHANGE_THRESHOLD=40` を割らない（最小 44.4〜45.7）。既定の対戦では
政治事件が mvp と japan47 で 0 件、japan_hex で 1〜5 件しか起きず、
3 シナリオ × 3 seed の 9 回で、ストライキ・反乱・資本逃避・政変は
それぞれ 0 回である（japan_hex の 1〜5 件は抗議と独立運動）。

原因は構造で、**影響力の 45%（軍部 0.20・財界 0.15・官僚 0.10）が、この
2 つのつまみで下がらない。** 軍部にいたっては徴兵で上がる。反乱と資本逃避は
この経路では到達不能である。

2026-09-12 に**コードではなく記述のほうを実態に合わせる**と決めた。§12 の
倒閣は戦死と戦災という第 3 の要因を伴って初めて成立し、勝ち続けている国は
政治的には倒れない。数値と却下した代案は `docs/future-work.md` にある。

**この項目を「成立している」に戻す前に、必ず測ること。**

### 残件

`docs/future-work.md` を参照。観測ベクトルのグラフ構造化、TLS 対応、
艦隊移動の hostile 判定、カメラのフィットの余白、陸軍の中の兵科（歩兵・機甲・
砲兵の区別）が未着手。輸送網の独立レイヤー化（Phase 9）・japan_hex の小国の
永久破綻・兵科としての航空戦力（Phase 10）は解決済み（小国の破綻は無条件の
兵力下限が原因だった）。観測ベクトルは Phase 10 で航空・制空権・ノード状態が
加わり長さが変わった（mvp 505・japan47 2223・japan_hex 11411 floats）。

### 特に注意: 試合時間の長さは欠陥ではない

japan_hex が 720 日で決着しないこと自体は仕様である。企画書 §23 は
「戦争に勝った」ではなく「一つの歴史が生まれた」ことを目指すと述べており、
§2 の主題も戦争そのものではなく国家システムの維持である。
Hearts of Iron 系の重厚さは、補給が細り政治が軋み戦線が動かない期間そのものが
体験の中身であることに由来する。

**早期決着を目的とした調整をしてはいけない。** 一度これを課題として記録し、
ブロック外交で解決しようとしたが結果はむしろ遅くなった。実際に修正すべき
だったのは「勝利が原理的に到達不能」という別の欠陥だった。

判定基準はこう置く。

    ×  720 日以内に決着する
    ○  勝利が到達可能である／各条件が規則どおりに発火する／戦争が実際に起きている

### 同様に: supply_ratio 0.4 前後は仕様である（2026-09-11 に決定）

`produced/demand` が 0.43、`supply_ratio` の中央値が 0.39〜0.43 で推移する。
**これは破綻ではない。** 0.43 で壊れるものはなく、消耗が始まる 0.25 にも余裕が
ある。戦闘力の `0.35+0.65*supply` と組織力回復の `0.3+0.7*supply` という下駄は、
満たされない前提で置かれた形をしている。補給が細る期間そのものが体験の中身だと
いう上記の判断と同じ筋である。

測定で分かっている仕組みは `docs/future-work.md`「需要が生産を上回る理由」に
ある。天井は 2 つ（`unit_cap` が軍需品の生産能力と無関係であること、輸送網が
在庫と独立に絞ること）で、落ち方は day 31〜90 の段差である。

**残っている乖離が 1 つある。** `HeuristicAgent::recruit` は
`supply_ratio < DISBAND_SOLVENCY_SUPPLY_RATIO`（0.5）で徴募を止めるので、
AI 自身の「健全」の基準は 0.5 である。実態は常にその下にあり、**AI はほぼ常時
徴募を控えている。** 値を実態に合わせると歯止めがなくなるので、合わせずに
乖離のほうを記録する。

**この数値を改善しようとして定数を動かさないこと。** Phase 11 はこの現象に
対して確かめずに対策を選び、丸ごと狙いを外した（別の品目を分けた）。
